name: Deploy GitHub Pages

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "index.html"
      - "styles.css"
      - "app.js"
      - "properties.html"
      - "data/**"
      - "food-intel/index.html"
      - "food-intel/iran.html"
      - "food-intel/data/**"
      - ".github/workflows/pages.yml"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare static site
        run: |
          mkdir -p _site/food-intel/data
          cp index.html styles.css app.js properties.html _site/
          cp -r data _site/data
          cp food-intel/index.html _site/food-intel/index.html
          cp food-intel/iran.html _site/food-intel/iran.html
          cp food-intel/data/news.json _site/food-intel/data/news.json
          cp food-intel/data/signals.json _site/food-intel/data/signals.json
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
